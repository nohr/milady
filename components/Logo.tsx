import { Text } from "react-native";

export default function Logo() {
  return (
    <Text
      style={{
        color: "#fff",
        fontSize: 24,
        fontWeight: "bold",
        position: "absolute",
        top: 100,
        zIndex: 1,
      }}
    >
      milady ♪
    </Text>
  );
}
