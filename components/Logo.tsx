import { Text } from "react-native";

export default function Logo() {
  return (
    <Text
      style={{
        color: "#fff",
        fontSize: 24,
        fontWeight: "bold",
        display: "flex",
        justifyContent: "center",
        // position: "absolute",
        // top: 100,
        // zIndex: 1,
      }}
    >
      milady ♪
    </Text>
  );
}
